# "mock" (par défaut) simule le serveur via axios-mock-adapter.
# Passe à "api" une fois le vrai backend prêt : httpClient.js n'installera
# alors plus le mock, et axios parlera vraiment à VITE_API_BASE_URL.
VITE_AUTH_BACKEND=mock
VITE_API_BASE_URL=http://localhost:4000/api